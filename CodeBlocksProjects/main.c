#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    double bound,sum,x;
    sum = 0;
    scanf("%lf",&bound);
    An = 1;
    for (i=1;i<=n;i++) {
        s = s + An;
        An = pow(x,2)
    }
    return 0;
}
